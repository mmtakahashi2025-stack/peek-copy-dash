project_id = "vpxbaxjwppiyodjzqsqo"

[functions.fetch-sheets]
# Disabled platform JWT verification due to ES256 token incompatibility in this runtime.
# We still enforce authentication inside the function via supabaseClient.auth.getUser().
verify_jwt = false

